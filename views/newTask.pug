extends layout

include mixins/createTask

block content
    .main-content
        h1 #{pageName}

        if errors
            each error in errors
                .alert= error.text
        +createTask(task)
